/*
*
* commands/ : this directory might contain individual files
* for each command that the bot supports, such as /start and /help.
*
* */

// bot.onText(/\/ask/, (message) => {
//     bot.sendMessage(message.chat.id, 'Ask received')
// })